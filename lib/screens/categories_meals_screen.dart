import 'package:flutter/material.dart';

class CategoriesMealsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    //Scaffold: andaime
    return Scaffold(
      appBar: AppBar(
        title: Text('Receita'),
      ),
      body: Center(child: Text('Receitar por categoria')),
    );
  }
}
